<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="theme-color" content="#f8ae2c"/>
    <meta property="twitter:title" content="{{ title }}"/>
    <meta property="og:title" content="{{ title }}"/>
    <meta property="og:description" content="{{ description }}"/>
    <meta property="description" content="{{ description }}"/>
    <meta property="twitter:image" content="{{ icon }}"/>
    <meta property="og:image" content="{{ icon }}"/>
    <meta name="application-name" content="AI Dungeon"/>
    <meta property="og:site_name" content="AI Dungeon"/>

    <link rel="canonical" href="{{ link | safe }}>"/>
    <meta property="og:url" content="{{ link | safe }}"/>

    <link rel="icon" href="{{ icon }}"/>
    <meta name="redirect" content="{{ link | safe }}"/>

    <title>{{ title }}</title>

    <link type="application/json+oembed" href="{{ oembed | safe }}"/>

    <!-- Ensure that directly loading this page results in redirect (should happen at the HTTP level) -->
    <script>window.location.replace("{{ link | safe }}");</script>

    <!-- Okay you heathens, have a nice looking page -->
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body class="embed profile">
<h6>AI Dungeon Profile</h6>
<h2><a href="{{ link | safe }}">{{ title }}</a></h2>
<p>{{ description | cap(350) | p }}</p>
<img alt="Profile icon for user '{{ title }}'" src="{{ icon }}"/>
</body>
</html>